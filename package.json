{
  "name": "ust_buysell_poc",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "dependencies": {
    "@cap-js/postgres": "^2.2.0",
    "@sap/cds": "^9",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "[postgres]": {
        "db": {
          "kind": "postgres"
        },
        "auth": "dummy"
      },
      "[sqlite]": {
        "db": {
          "kind": "sqlite"
        },
        "auth": "dummy"
      }
    }
  },
  "private": true
}